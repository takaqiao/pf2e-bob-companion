# 规则与实现依据

对应版本：伴随模组 0.4.1，BoB 1.0.0、PF2e 8.5.1、Foundry 14.368。

依据《亵渎堡垒》冒险正文及官方 Foundry VTT 冒险模组核对。本包仅包含实现所需的规则数值、适用条件和独立实现代码，不附送冒险全文、地图或官方物品数据。

## 源文定位

《亵渎堡垒》冒险正文中的相关章节：

- 冒险总述中的夜幕与日长规则：三项夜幕修正及日夜边界。
- 第1–9章各章的环境专节：日出日落、察觉与远程打击修正，以及需要GM判断的环境危险。
- 第9章环境专节：最后一次14:00日出日落；放回徽章的剧情雷击为独立事件。
- 终局章节：风暴停止与解除诅咒的条件。

官方冒险的六个天空效果依次为：

| 章 | 原始Item ID | 原生视觉察觉／远程打击 |
|---|---|---|
| 4 | KAI0Cx2ys0P56hZw | −2／无；≥50英尺的隐蔽说明 |
| 5 | 4azrweHGcJ1XoBqo | −1／无 |
| 6 | 9oiPR5aNEpQTzgfD | −2／−1 |
| 7 | XVFJTwaE9P3H5IRk | −3／−2 |
| 8 | DGrF0tPpGZcNNQUN | −3／−3 |
| 9 | lff614VwbcqbXxtO | −4／−4；描述仍写−3 |

本模组复用这些效果采用的原生 `FlatModifier`、`perception`、`ranged-strike-attack-roll`、`item:trait:visual` 机制及第四章距离提示。按正文补充户外／章节／时间条件，第九章明确取消视觉限定；只在受管上下文临时替代原有天气规则。

## 与现有自动化衔接

官方 `_js/campaign/data.mjs` 派生 `campaign.activeArea.chapter`，表示当前最远已解锁区域。`manager.mjs` 的 `onUpdateCampaignData` 负责官方天气与PF2e渐变光照；未发现夜幕三项数值规则。已有六项天空效果本身没有章节和户外自动启停。

读取 `game.pf2e.worldClock.worldTime` 的日内时刻，包含PF2e建世时间偏移；不能直接用 `game.time.worldTime % 86400`。以24小时制判定时段，不修改 Calendaria 的时间、月相、走时速率或既有视觉天气／音效设置。

第一章使用章节专节07:30—19:30。第九章无持续白昼；最后一次升落由GM处理叙事。第六章默认停雨13:00—14:30是模组默认设置，不是原著给定时刻；两项检定减值遵循正文“户外下雨期间”的适用条件。

## 实现方式

昼夜／天气部分用 libWrapper 同步包装 `Actor.prepareRuleElements`，构造 `CONFIG.PF2E.Item.documentClasses.effect` 的仅内存实例，规则照常进入 PF2e 合成统计。这部分不会向 actor.items 插入数据，也不会改写官方物品规则。0.3.0 的冒险助手则在明确结算后创建或更新原生效果，并按具体用途修改实际物品数量或魅影阶位。

兼容版本为 Foundry 14.368／PF2e 8.5.1。PF2e 更新角色准备流程后，需要重新核对这个接口。

源码中的 `tests/` 包含规则与上下文判定测试；运行方法见 `README.md`。

## 日历同步

0.2.0 在 0.1.1 的移动性能修复上加入可选 Calendaria 1.4.2 适配器。只调用核实过的原生区域/天气接口；规则引擎与日历共享环境判定，普通棋子移动不触发日历写入。昼夜、散雾、雨歇和严寒时段从同一环境状态读取。

天气接口会按原生流程记录真正的天气变化。由于 Calendaria 默认手动改天气会清除并重建预报，模块在该选项启用时先提示，通过明确的兼容操作关闭该选项后才自动更改天气；不自行清理预报。每日生成的允许天气与有效温度范围保留，GM 手工覆盖优先。

具体温度、风速、第一章晴/少云权重以及午夜前半小时飞雪来自既有世界配置的 GM 校准。模块不附送世界历法、节日日志或商业冒险资产，也不把旧的整世界安装脚本重复挂到启动事件。

## 四类冒险助手

实际地点、剧情前提及目标由 GM 确认。各工具使用的规则如下。

| 领域 | 依据与实现范围 |
|---|---|
| 休息与噩梦 | 依据冒险的首夜、章节首夜及后续休息规则。首次及新章节首夜按必定触发处理，其他休息使用 DC 16 平检；需要时请求当前角色意志检定。跟踪每名角色每章的大失败次数、24 小时当前效果和首次惊惧加值。既有战役须先由 GM 初始化，取消未确认休息不消费首夜。 |
| 恩惠 | A14、A19 使用个人一周冷却；C45 三项共用 24 小时冷却，其技能效果还要求使用后 8 小时内、白天及人在绑定房间。A19 以完成活动期间是否跨日出判断当前制作恩惠；疾病与净化留作 GM 确认。G4 记录已决定授予的单次恩赐、持有者、转授和消费。 |
| 环境危险 | 污秽空气成功豁免取得的免疫跨房间保留，连续离开地牢超过 8 小时才失效，返回地牢会重新开始离开计时。高塔危险按队伍户外累计每 600 秒产生一次待办，全员离开或风暴停止时暂停；跳时合并待办，目标与伤害不自动结算。 |
| 魂心 | 三种可用于强化的等级分别要求魅影当前 0、1、2 阶，提升一阶并给每名 PC 增加 1、2、3 点最大 HP。每日临时 HP 按普通／高等／上等及昼夜分别为 6／12／18 或 3／6／9，持续最多 8 小时，不消耗物品。纯净及天使类别留作剧情使用。 |

高塔户外计时在全员离开后暂停，已累计的时间保留。先为地牢和高塔配置 Region，进出追踪才会生效。清醒梦等研究提示仍需在已有研究场景中处理，沿用该场景的来源额度记录。

魂心阶位通过原生 `support-rank` 开关更新；永久 HP 复用原生计数效果，每名角色维护一个完整累计值。确认前会展示已有奖励基线、实际数量和受益角色；新角色可补齐已获得的队伍奖励。官方旧战役计数字段上限 21，助手以自己的完整记录和角色效果为准，不截断实际 HP。事务先保存记录再操作文档，失败按已完成步骤恢复；撤回最近奖励会先检查后续记录和实际物品状态。

临时 HP 使用原生 `TempHP` 规则，等级及昼夜由实际物品和预览解析，已解析的条件不会再要求隐藏选项。恢复失败事务时按原定结束时刻计算剩余时长，不重新延长 8 小时。原生效果不可用时显示待处理原因，不替换为未经核对的规则。

## 状态、暂停与可见范围

世界记录使用不公开配置界面的设置项，修改和结算由当前主 GM 串行执行。普通移动、无实际变化的时钟更新及重复操作不应重复发放效果；副 GM 不并行结算。取消启用后暂停自动接入与追踪，保留已获效果、使用记录和冷却；已有原生效果按实际条件由 GM 清理。

玩家界面显示当前检定和已获得的效果；GM 工具与未揭示内容留在 GM 界面。脚本及 API 的访问范围由 Foundry 权限配置决定。

测试覆盖规则判定、权限、重复操作和中断恢复。运行方法见 README；升级 Foundry、PF2e 或搭配其他自动化模组时，应先在测试世界核对。
